# 1. Install dependencies
cd server
npm install

cd ../client
npm install

# 2. Add your Google Maps API key in App.js if needed
     (Left my API key in)

# 3. Run the servers
cd server
node server.js

cd ../client
npm start

